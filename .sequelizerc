const ENV = process.env.NODE_ENV || 'development';

module.exports = {
  config: 'config/database.js',
  'migrations-path': 'src/migrate,
  'models-path': 'src/models',
  'seeders-path': 'src/seeds'
};
